package com.evolution.analysis.contract.source;

/** Declared source role; discovery of these roles belongs to the later workspace adapter. */
public enum SourceClassification {
    MAIN,
    TEST,
    GENERATED_MAIN,
    GENERATED_TEST,
    OTHER
}
