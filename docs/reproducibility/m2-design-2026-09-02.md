# M2 Design, Oracle Pilot and Antigravity Readiness

> Historical preflight-stage record. Later human instructions authorized implementation using the mature contracts, then paused work and discontinued Antigravity. The approval blockers, dispatch instructions and exact-next-task text below describe the earlier stage and are superseded by the [pause handoff](m2-pause-2026-09-02/README.md) and [current state](../current-state.md). Its earlier verification evidence retains its stated scope; it is not verification of later drafts.

Date: 2026-09-02. **M2 started; production frontend and G2 remain incomplete.** The user requested M2 work using the Codex–Antigravity orchestrator and supplied the standalone CLI path during the task.

## State, scope and artifacts

Starting HEAD: `83797e840e414bf99a0f71117892da355d94be55`. M1/G1 was complete historically, and JavaParser/SymbolSolver had human approval. The checkout already contained substantial uncommitted governance changes. The coordinator captured 48 existing changed/deleted/untracked file states, the working patch and empty staged patch under `out/m2-design-20260902/control/`. All 48 were verified unchanged before the intentional operational-state update; final preservation excludes only that authorized `docs/current-state.md` edit.

| Work | Actual result |
|---|---|
| [M2 contract proposal](../architecture/m2-semantic-frontend.md) | Concrete D1 identity, D2 port/evidence/entities and D3 coordinate decisions; 18-category catalog; origin/uncertainty/denominator rules; production implementation order and acceptance boundaries |
| [JDK oracle pilot](../../benchmarks/m2-ground-truth/README.md) | Standalone Java/Python package, six source fixtures, seven compilation groups, 34 occurrence labels and three identifier relations; no third-party dependencies |
| [Antigravity package](m2-antigravity/README.md) | Two bounded adversarial prompts, exact required-read hashes, model/effort requests, no-write ownership and lifecycle/receipt rules |
| Durable state | `docs/current-state.md` records M2 design progress, new evidence and remaining decisions. Roadmap scope/order and existing M1/production sources are unchanged by this task |

No commit, push, dependency upgrade, global instruction change or production adapter was made. The only installed-runtime side effect was normal initialization of the existing Antigravity CLI's data directory during the permitted model-list operation; no software was installed.

## Fresh verification and evidence

| Command/check actually run | Observed result and claim boundary |
|---|---|
| `.\mvnw.cmd --version` | Maven 3.9.16, Oracle JDK 21.0.12.1, Windows 11 |
| `.\mvnw.cmd -B -ntp verify` | Exit 0; analyzer 25 tests + backend 1 test, zero failures/errors/skips. This verifies the existing reactor/M1 state; the standalone pilot is outside the reactor. No clean-build claim |
| `python benchmarks/m2-ground-truth/run_oracle.py --jdk 'C:/Program Files/Java/jdk-21.0.12.1' --output-root out/m2-design-20260902/integrated-oracle-runs` | Exit 0; 34/34 occurrence labels, 30 adjudicated targets, four withheld error-case targets, three passing identifier relations; all ten subprocess commands exit 0 |
| Integrated raw-result inspection | Compiler diagnostics present for ambiguity, missing type and malformed syntax; zero admissible bindings in each erroneous compilation. Original source offsets and declaration/platform origin evidence retained |
| Candidate integration | Ten new package files copied from the isolated worker candidate; every source/destination SHA-256 matched; scratch caches excluded |
| Evidence archive | Eleven generated artifacts copied byte-for-byte; seven raw stdout files only changed filename extension to `.json`. [Archive receipt](../../benchmarks/m2-ground-truth/results/pilot-2026-09-02/archive.json) records every hash |
| Independent design review | Three P1 issues and one P2 issue corrected and re-read; verdict APPROVE for human review, not approval to adopt production contracts |
| Worker pilot negative controls | Wrong target, admitted recovered ambiguity, extra declaration-only fixture and empty corpus were rejected. Coordinator inspected the actual runner and receipt; these negative controls are worker-executed, not claimed as a separate coordinator rerun |
| Structure/diff checks | Existing governance link checker passed (38 documents, 80 links before state update); final new-document/link/hash/preservation checks recorded below. No product behavior claim follows from Markdown checks |

Integrated run: `out/m2-design-20260902/integrated-oracle-runs/20260902T143606-493e3361/`. The [archived summary](../../benchmarks/m2-ground-truth/results/pilot-2026-09-02/summary.json), raw stdout, exact command list, preregistration and manifest are versionable inputs to review. Full local attempts also retain original inputs, instrumentation, stderr and compiled oracle classes. Old attempts were not overwritten.

**CONFIRMED within the pilot:** composed/decomposed method names bind to distinct declarations; U+200C/Unicode-escape variants bind to their corresponding plain names; selected overload/generic/varargs targets and original UTF-16 spans match the labels. A `javax.lookalike` project declaration is identified through source membership rather than its package prefix. `javac` exposes a recovered method element in the deliberately ambiguous case; a returned element is therefore insufficient to validate a binding when compilation has errors.

These are assistant-authored, source/JLS-based labels frozen before oracle execution. The pilot checks method calls and selected language/evidence risks. It does not evaluate the full M2 category matrix, caller/declaration matrix, dependency-JAR origins, multi-module classpaths, complete unsupported/error handling, production M1 mapping, canonical key generation, scale or performance. Independent human label review and production adapter validation remain necessary.

## Design review and decisions

The independent Codex reviewer inspected repository contracts/source, the experimental comparator and the actual proposal. Corrections made before handoff:

- Recursive member-type keys retain local/anonymous owner anchors, preventing two same-named local classes' members from colliding.
- Field, enum-argument and class-block initializer ownership is explicit across relationship categories.
- Derived implicit relationships use an evidence/status/derivation envelope without fabricated explicit source occurrences.
- Source identity offsets are zero-based original UTF-16. The first catalog uses ordinal zero for unique explicit facts; duplicate extractor emissions are diagnosed and fail evaluation.

Final independently inspected design SHA-256: `29dd7442b07a0152c2890d7caea9961e37724f5706aa182599754efc8a900175`. Review report: `out/m2-design-20260902/independent-audit/audit.md`. Its initial findings and final repair verification remain distinguishable. This reviewer did not execute Gemini or the pilot and did not authorize the schema.

**OPEN HUMAN DECISION:** approve or revise D1–D3 in the proposal. These refine canonical identity and extend the entity/evidence schema, so AGENTS.md's consequential identity/schema decision rule applies before dependent production edits. The JavaParser choice is already approved and is not being reopened. Directory/module names, isolated pilot implementation and other reversible details were handled locally.

The legacy comparator is explicitly quarantined from M2 accepted labels and G2 totals. Its raw results remain unchanged; package-prefix origin heuristics, pseudo callers, mapper kind/digest/span errors and unadjudicated compiler recovery must not become production evidence.

## Antigravity: verified capability and actual blocker

**CONFIRMED from local help:** `C:/Users/Admin/AppData/Local/agy/bin/agy.exe` is version 1.1.24 and supports pinned model, effort, plan mode, structured output, explicit conversation and finite print timeout. `agy models` completed with exit 0 after the supported escalation allowed its normal data-directory initialization. Selectors included Gemini 3.1 Pro High and Gemini 3.7 Flash High. The selected reasoning model, token use, account quota/reset and a live repository round-trip are still unverified.

The initial sandbox model-list failure was an environment permission failure, not a Java/M2 defect. The later request to run the read-only handshake with Gemini 3.1 Pro High was **rejected before execution by automatic approval review**, because the prompt would send repository documents/diffs/untracked metadata to an external service without sufficiently specific payload authorization. The user was asked to authorize M2 repository processing, excluding secrets/credentials. No workaround, indirect dispatch or repeated unapproved request was attempted.

Operating mode remains **ASSISTED/PREPARED**, not DIRECT. The rejected handshake has no conversation/run ID. Its fresh nonce and concrete prompt are preserved under `out/m2-design-20260902/control/handshake.txt`; a future approved attempt must use a new nonce and refreshed inputs. No Gemini worker was started, and no external writer has ownership to release. The existing CLI's normal local server process is not evidence of a running model job.

The pending design review requests `gemini-3.1-pro-high`, effort `high`; the separate oracle challenge requests `gemini-3.7-flash-high`, effort `high`, with a model-change handshake. These are actual discovered selectors chosen for documented task capabilities, not verified run identities or claims of universal model superiority. No `/boost` or native team run was attempted.

## Task closure and exact continuation

Local children `m2_independent_design_audit` and `m2_oracle_pilot` both completed. They are Codex subagents, not Gemini substitutes presented under another name. The former was read-only except its report; the latter wrote only an isolated scratch candidate/run directory. Codex inspected and integrated the accepted artifacts and reran the pilot in the repository.

Remaining blockers are specific: external repository-payload consent and a verified Antigravity handshake; the human D1–D3 identity/schema decision for production work. Full-category labels, independent frontend verification and M3/G2 evidence remain work, not passed gates. No scope or performance threshold was silently approved.

**Exact next task:** review the concrete D1–D3 proposal and repository-payload permission, then implement the approved neutral contracts plus golden/negative tests and isolated JavaParser adapter in bounded M2 increments. Refresh the dispatch manifest, verify a nonce/model/workspace round-trip and supervise real Antigravity review before assigning an external writer. Do not start M3 or claim M2/G2 completion from this pilot.
