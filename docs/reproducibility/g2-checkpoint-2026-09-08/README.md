# Superseded Invalid G2 Attempt: PC-Shop

This directory preserves the first 2026-09-08 attempt for audit only. It is **not valid Gate evidence**: the runner used an incorrect repository revision, hard-coded Java/platform values, omitted external parent/BOM acquisition, included repository-internal `.git`/build output, fabricated a reactor JAR, initialized no closed 18-category denominator, and the renderer silently failed because Jackson was not declared. The empty `{}` table therefore cannot support a coverage or correctness claim.

The corrected, independently named rerun is preserved in `../g2-checkpoint-2026-09-08-v2/`. G2 remains **WITHHELD**.
