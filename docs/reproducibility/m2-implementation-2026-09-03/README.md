# M2 production frontend slice — 2026-09-03

**CONFIRMED by execution:** final root `clean verify` passed: **62 tests, zero failures/errors/skips** (analyzer 38, adapter 23, backend 1). The frozen corpus checks all **27 type/callable declarations and 23 invocations**, including exact identities, callers, targets, origins, spans, ordinal zero, missing dependencies, unexpected facts and deterministic reruns. **M2 remains incomplete; G2 is not passed.**

## State and work

Started from the human's clean checkpoint `8f7582e`. M1/G1 was already implemented; M2 had contracts and archived drafts, without a production port/adapter. The human resumed implementation using Codex only and existing D1–D3 authorization. No Antigravity/Gemini, automation, commit, push or M3 work occurred.

Implemented in `analyzer/src/main/java/com/evolution/analysis/frontend/`: Unicode-safe structural Java names/parameter erasure; strict immutable source bytes; typed platform/JAR handles; request-plan and manifest checks; declaration/observation/source/category contracts; consistent deterministic result validation. M1 entity kinds append `RECORD_COMPONENT`, `LAMBDA`, `INITIALIZER`; existing M1 golden identity inputs are unchanged.

Implemented isolated `analyzer-javaparser`: in-memory source indexing, verified running JDK 21 platform, immutable verified JAR snapshots, declaration identities and method/explicit-constructor occurrences. Lexical owners include local/member types, field/block initializers and lambdas. Missing bindings, ambiguity, parse errors, unsupported constructs, duplicate declarations/facts and adapter errors remain explicit. Known declaration identity can coexist with partial type information. Parser objects remain in the adapter.

The root POM integrates the adapter; the analyzer POM/README describe the boundary. Tests and byte-preserved fixtures live with their owning module. [Current state](../../current-state.md) and [M2 contract implementation notes](../../architecture/m2-semantic-frontend.md) now reflect this scope. Roadmap and project context were unchanged.

## Verification and evidence

Environment: Windows, Oracle JDK **21.0.12.1**, wrapper-pinned Maven **3.9.16**, JavaParser/SymbolSolver **3.26.1**, JUnit **5.11.0**. Gson **2.11.0** is test-only for reading independently authored fixture labels. Exact runtime output: [toolchain](toolchain.txt).

Final command, from the root with supported execution approval for the user cache:

```powershell
$env:MAVEN_USER_HOME = 'C:\Users\Admin\.m2'
.\mvnw.cmd -o -B -ntp '-Dmaven.repo.local=C:/Users/Admin/.m2/repository' clean verify
```

- [Final raw clean verification](clean-verify.txt): all four reactor projects succeeded; [suite counts](test-summary.json) are extracted from the final Surefire XML.
- [Tested source/configuration hashes](tested-source-hashes.json): exact source, test, fixture and POM inputs of the verified state. Generated build output is excluded.
- [Raw fixture outputs and manifests](results/): copied from `analyzer-javaparser/target/m2-evidence/`, generated by `FrozenFixtureTest`. [Result hashes](result-hashes.json) identify copied bytes. Rerunning the test regenerates `target/m2-evidence`; preserve this dated archive and capture later runs separately.
- The fixture test manifests use explicitly named `test.fixture` component digests for analyzer/rules/schema. They identify the harness plan and exact sources/platform, not a shipped analyzer binary or real-project build model. The tested-source hashes and pinned parent commit identify this implementation.
- Focused commands actually run included `-pl analyzer -am test`, `-pl analyzer-javaparser -am test`, and selections for `JavaSymbolNameTest`, `FrontendContractTest`, `FrontendResultTest`, `SemanticBoundariesTest`, `ResolutionInputsTest` and `OriginalSourceTest` (with `-Dsurefire.failIfNoSpecifiedTests=false` only when the selected test intentionally lives in one reactor module). The online adapter run acquired a missing transitive artifact; subsequent final verification was offline.

The first root baseline verify passed 26 existing tests. New test compilation then reproduced the sandbox JUnit-cache access failure. Running the same wrapper command through supported execution approval reached real tests; no cache copying or permission weakening was used. Six identity stubs and six adapter/coordinate stubs produced actual missing-behavior failures before implementation. Later regressions observed failures for source-set identity collisions, ledger/coverage inconsistency, foreign category prefixes, Unicode escape end spans, missing declaration type status and lexical ownership; corrected cases passed.

One archived coordinate-test expectation was wrong: the third `hit()` begins at UTF-16 column **46**, independently recounted from its source, rather than 47. Only the integrated test was corrected; the [pause archive](../m2-pause-2026-09-02/README.md) remains unchanged.

The [first clean attempt](verify.txt) could not delete `ErasedType.class`, which had been created under the sandbox account. Read-only inspection confirmed ordinary file attributes and that ownership; no lingering JDK probe was retained. [Maven clean under its owning sandbox context](clean-owner-context.txt) succeeded. The subsequent complete approved `clean verify` also succeeded. No ACLs were changed. Surefire emitted a Windows cross-drive boot-manifest warning; tests executed and passed, without applying its suggested classpath-check-disabling flag.

## Independently reviewed scope and remaining limits

A Codex reviewer independently checked the frozen labels against source: all four source hashes and all 50 declaration/occurrence spans matched. Registered invocation outcomes are **18 method calls (17 resolved, 1 unresolved)** and **5 explicit constructors (4 resolved, 1 unresolved)**. The independent JDK call in the missing-dependency fixture remains resolved while its source remains partial. The earlier oracle pilot was not rerun or used to approve recovered compiler bindings.

Read-only Codex source reviews found and closed source-plan identity, mapped-ledger consistency, source/category coverage, category-prefix, lambda initializer-owner, enum-body and record-body issues. Final scoped review approved the fixes. This is independent bounded review, not full M2 acceptance or an independent rerun of all tests.

Additional executable controls cover composed/decomposed and ignorable Java identifiers, generic erasure/varargs/inherited targets, ambiguity, duplicate declarations, original CR/LF/CRLF/tab/supplementary/Unicode-escape coordinates, real dependency origins/order/removal, changed artifact digests, source permutation and application-classpath isolation. Test JARs compile only fixed test-owned Java with `--release 21 -proc:none` and explicit classpath/sourcepath; no target Maven lifecycle executes.

**PROVISIONAL support boundaries:**

- Record source attribution is `UNSUPPORTED`: the pinned resolver throws when indexing `RecordDeclaration`. Its source and observations remain visible; compact-constructor calls never fall back to a TYPE caller.
- Constant-specific enum bodies and implicit source callables are withheld. No invented implicit declaration spans or anonymous enum owners are emitted.
- Most of the 18 relationship categories, recursive structured type detail and derived-relationship records remain unimplemented. Their catalog rows remain explicit; zero attempted for an unsupported row is not measured absence of references. Full M2 denominators remain open.
- Inputs are one exact module/source-set request, the verified running JDK 21 image and explicit single-release JARs. Alternative JDK views, module outputs, multi-release JARs and manifest classpaths are rejected. Limits are explicitly unbounded; no scale/performance claim is made.
- No new full compiler-oracle run, real-repository acceptance, second-platform build or M3 build acquisition was performed. Existing raw benchmark evidence remains unchanged.

No new human decision or execution blocker remains for the next authorized slice. The 3.26.1 pin is a bounded tested implementation choice, not evidence of complete Java 21 support. Next: implement neutral recursive Java type detail and the parameter/return/field type relationship slice with independent labels, reusing verified identities, origins and source evidence. Keep record/implicit-member gaps explicit until their own bounded semantic implementation.
